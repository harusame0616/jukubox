name: マイグレーションテスト

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: migration-test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  migration-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: 変更ファイルのチェック
        uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            migrations:
              - apps/api/migrations/**
              - packages/supabase/**

      - name: Supabase CLI のセットアップ
        if: steps.filter.outputs.migrations == 'true'
        uses: supabase/setup-cli@v1
        with:
          version: 2.92.1

      - name: Supabase の起動
        if: steps.filter.outputs.migrations == 'true'
        run: supabase --workdir packages/supabase start

      - name: golang-migrate のインストール
        if: steps.filter.outputs.migrations == 'true'
        run: |
          curl -L https://github.com/golang-migrate/migrate/releases/download/v4.18.1/migrate.linux-amd64.tar.gz | tar xvz
          sudo mv migrate /usr/local/bin/migrate

      - name: マイグレーションの実行
        if: steps.filter.outputs.migrations == 'true'
        run: |
          migrate -path apps/api/migrations \
            -database "postgresql://postgres:postgres@127.0.0.1:54322/postgres?sslmode=disable" \
            up
