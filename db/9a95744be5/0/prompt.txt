/ticket チケットを整理したいです。コース作成は WEB からと CLI からできるようにする予定です。 CLI はフォルダ内に yaml ファイルを配置することで自動でファイルを読み込みアップロードするようなものを想定しています。WEB ではフォームを用いて登録できるようにします。このチケットでは web ページの対応を行います
コンテンツはYAML形式の入力ですが、YAMLをそのまま直接入力するのではなく、YAMLのセクションごとにテキスト入力欄を用意して組み立てて送信するような形で考えています。他にUXが良いインターフェースがあれば検討してください。

---

c で行こうかな。ちょうど良いライブラリとかってある？

---

OK チケット更新後実装して

---

チケット更新して

---

authors と user_id は直接紐付けず中間テーブルで実装してください

---

ファイル名ってなんで必要なの？

---

削除して

---

各分類のシードをもっと拡充させて欲しい

---

カテゴリーをサブエージェントに切り出してもっと精査して。重複や不要なもの重要なものが漏れていないか３回ループを繰り返して完璧なものにして

---

フォームでコースタイトルを入力した後 tab キーやフォーカスが外れると他のフォームが赤くなってしまう（バリデーションエラー表示）サブエージェントで調査・修正してください

---

基本 SQL で組み立てない方がいいの？他のコードでは sql で json 組み立ててるところが多いんだけど

---

このコードベースでというより一般論としてどっちがいいかという意味で聞いている